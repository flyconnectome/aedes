% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-aedes-neurons.R
\name{aedes_soma_position}
\alias{aedes_soma_position}
\title{Look up the soma position for one or more Aedes neurons}
\usage{
aedes_soma_position(
  ids,
  method = c("auto", "flytable", "nucleus"),
  units = c("nm", "raw"),
  version = NULL,
  timestamp = NULL
)
}
\arguments{
\item{ids}{Root IDs or a FlyTable query string accepted by \code{\link[=aedes_ids]{aedes_ids()}}.}

\item{method}{One of \code{"auto"}, \code{"flytable"}, \code{"nucleus"}. With \code{"auto"}
(the default) the function first tries FlyTable's \code{soma_xyz} and then
falls back to \code{\link[fafbseg:flywire_nuclei]{fafbseg::flywire_nuclei()}} for any neuron that lacked a
recorded soma. Naming a single method restricts the lookup to that source.}

\item{units}{Units of the returned coordinates: \code{"nm"} (default) or \code{"raw"}
voxel coordinates. FlyTable's \code{soma_xyz} is always stored as raw voxel
coordinates and is converted to nm via \code{\link[=aedes_raw2nm]{aedes_raw2nm()}} before being
returned (unless \code{units = "raw"}).}

\item{version, timestamp}{Optional CAVE materialisation selectors passed
through to \code{\link[=aedes_meta]{aedes_meta()}} and \code{\link[fafbseg:flywire_nuclei]{fafbseg::flywire_nuclei()}}.}
}
\value{
A data.frame with one row per input id (in the same order), with
columns \code{root_id}, \code{x}, \code{y}, \code{z} (in the requested \code{units}), and \code{source}
(\code{"flytable"}, \code{"nucleus"}, or \code{NA}).
}
\description{
Returns the recorded soma position (in nm) for each input root id, using
FlyTable annotations and/or the FlyWire nucleus segmentation.
}
\examples{
\dontrun{
aedes_soma_position("class:DNa")
aedes_soma_position("class:DNa", units = "raw")
aedes_soma_position(c("648518347528739642", "648518347497973071"),
                    method = "flytable")
}
}
\seealso{
\code{\link[=read_aedes_neurons]{read_aedes_neurons()}}
}
