% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/partners.R
\name{aedes_partner_summary}
\alias{aedes_partner_summary}
\title{Summarise the synaptic partners of one or more Aedes neurons}
\usage{
aedes_partner_summary(
  rootids,
  partners = c("outputs", "inputs"),
  threshold = 0,
  version = NULL,
  timestamp = NULL,
  synapse_table = getOption("coconatfly.aedes.synapses", default = "synapses_v2"),
  ...
)
}
\arguments{
\item{rootids}{Query neurons in any form accepted by \code{\link[=aedes_ids]{aedes_ids()}} (root
ids or a FlyTable query string).}

\item{partners}{Whether to summarise \code{"outputs"} (downstream partners, the
default) or \code{"inputs"} (upstream partners).}

\item{threshold}{Only return partners connected by more than \code{threshold}
synapses (default \code{0}, i.e. all partners).}

\item{version, timestamp}{Optional CAVE materialisation selectors. When
supplied, query ids are first updated to the corresponding version /
timestamp with \code{\link[fafbseg:flywire_latestid]{fafbseg::flywire_latestid()}}, and the synapse query is
run against that materialisation. Give at most one of the two.}

\item{synapse_table}{CAVE synapse table to query. Defaults to the
\code{coconatfly.aedes.synapses} option (\code{"synapses_v2"}).}

\item{...}{Additional arguments passed on to
\code{\link[fafbseg:flywire_partner_summary]{fafbseg::flywire_partner_summary()}}. Power-user options include
\code{remove_autapses} (set \code{FALSE} to keep self-connections; see examples)
and \code{cleft.threshold}.}
}
\value{
A \code{data.frame} with one row per partner neuron. \code{query} holds the
query neuron root id and \code{weight} the synapse count; the partner root id
is in \code{post_id} when \code{partners = "outputs"} and \code{pre_id} when
\code{partners = "inputs"}. See \code{\link[fafbseg:flywire_partner_summary]{fafbseg::flywire_partner_summary()}} for the
full column description.
}
\description{
Tabulates the up- or downstream partners of a set of query neurons at a
chosen CAVE materialisation, returning one row per partner with the number
of connecting synapses. This is a thin Aedes-aware wrapper around
\code{\link[fafbseg:flywire_partner_summary]{fafbseg::flywire_partner_summary()}}: it resolves the input via
\code{\link[=aedes_ids]{aedes_ids()}}, points fafbseg at the Aedes segmentation, and updates root
ids to the requested \code{version}/\code{timestamp} before querying.
}
\examples{
\dontrun{
# downstream partners of a neuron, keeping only strong connections
aedes_partner_summary("720575940...", threshold = 4)

# inputs instead of outputs
aedes_partner_summary("720575940...", partners = "inputs")

# Power-user: query a specific CAVE materialisation rather than 'now'
aedes_partner_summary("720575940...", version = 1000)
aedes_partner_summary("720575940...", timestamp = "2024-01-01")

# Power-user: include autapses (self-connections). MBON11 is strongly
# autaptic, so its own root id appears among its downstream partners when
# remove_autapses = FALSE (the fafbseg default drops these).
mbon11 <- aedes_ids("cell_type:MBON11")
aedes_partner_summary(mbon11, remove_autapses = FALSE)
}
}
\seealso{
\code{\link[fafbseg:flywire_partner_summary]{fafbseg::flywire_partner_summary()}}, \code{\link[=aedes_ids]{aedes_ids()}}
}
