% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coords.R
\name{aedes_mirror}
\alias{aedes_mirror}
\title{Mirror Aedes neurons or points to the opposite side of the brain}
\usage{
aedes_mirror(
  x,
  method = c("tps", "landmarks", "symmetric"),
  units = c("nm", "microns"),
  subset = NULL,
  landmarks = NULL,
  raw = TRUE,
  url = .aedes_mirror_landmarks_url,
  vd = NULL,
  ...
)
}
\arguments{
\item{x}{An object to mirror.}

\item{method}{Mirror method. \code{"tps"} uses the bundled
\code{aedes_aedesSym_1000_tps.rds} registration to map into Aedes symmetric
space, flip across the X axis, then map back to original Aedes space.
\code{"landmarks"} uses paired landmarks in the original Aedes space.}

\item{units}{Coordinate units for \code{x} and the returned object.
The symmetric registration is defined in microns; nm inputs are scaled
before and after transformation.}

\item{subset}{Optional subset passed to \code{\link[nat:xform]{nat::xform()}}, for example when
transforming selected elements of a neuronlist.}

\item{landmarks}{Optional landmark data with \code{pointA} and \code{pointB} entries.
When \code{NULL}, landmarks are read from \code{url} using
\code{\link[fafbseg:ngl_annotations]{fafbseg::ngl_annotations()}}.}

\item{raw}{Whether landmark coordinates are in raw voxel space. When
\code{FALSE}, landmark coordinates are assumed to be in \code{units}.}

\item{url}{Neuroglancer URL containing paired mirror landmarks.}

\item{vd}{Optional voxel dimensions in nm. Advanced use; when \code{NULL} and
\code{raw = TRUE}, these are detected automatically.}

\item{...}{Additional arguments passed to \code{\link[nat:xform]{nat::xform()}}.}
}
\value{
A transformed object of the same kind as \code{x}.
}
\description{
\code{aedes_mirror()} mirrors objects supported by \code{\link[nat:xform]{nat::xform()}}, including
neurons, neuronlists, dotprops and coordinate matrices.
}
\details{
Mirroring requires the suggested \code{Morpho} package at runtime.
}
\examples{
\dontrun{
sk <- with_aedes(fafbseg::read_l2skel(aedes_ids("class:DNa")[1]))
sk.mirror <- aedes_mirror(sk, units = "nm")

plot3d(sk, col = "grey")
plot3d(sk.mirror, col = "red", add = TRUE)

dps <- with_aedes(fafbseg::read_l2dp(aedes_ids("class:DNa")))
dps.mirror <- aedes_mirror(dps, units = "microns")
}
}
