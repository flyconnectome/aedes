% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chunkedgraph.R
\name{aedes_xyz2id}
\alias{aedes_xyz2id}
\title{Find Aedes root or supervoxel (leaf) IDs for XYZ locations}
\usage{
aedes_xyz2id(
  xyz,
  rawcoords = FALSE,
  voxdims = aedes_voxdims(),
  cloudvolume.url = NULL,
  root = TRUE,
  timestamp = NULL,
  version = NULL,
  stop_layer = NULL,
  integer64 = FALSE,
  method = c("auto", "cloudvolume", "spine"),
  ...
)
}
\arguments{
\item{xyz}{One or more xyz locations as an Nx3 matrix or in any form
compatible with \code{\link[fafbseg]{xyzmatrix}} including \code{neuron} or
\code{mesh3d} surface objects.}

\item{rawcoords}{whether the input values are raw voxel indices or in nm}

\item{voxdims}{voxel dimensions in nm used to convert raw coordinates. The
default value uses the \code{\link[fafbseg]{flywire_voxdims}} function to identify
the value for the current segmentation (usually with success).}

\item{cloudvolume.url}{URL for CloudVolume to fetch segmentation image data.
The default value of NULL chooses the flywire production segmentation
dataset.}

\item{root}{Whether to return the root id of the whole segment rather than
the supervoxel id.}

\item{timestamp}{An optional timestamp as a string or POSIXct, interpreted as
UTC when no timezone is specified.}

\item{version}{An optional CAVE materialisation version number. See details
and examples.}

\item{stop_layer}{Which layer of the chunked graph to stop at. The default
\code{NULL} is equivalent to layer 1 or the full root id. Coarser layer 2
IDs can be a useful intermediate for some operations.}

\item{integer64}{Whether to return ids as integer64 type (more compact but a
little fragile) rather than character (default \code{FALSE}).}

\item{method}{Lookup method: \code{"auto"} and \code{"spine"} use the Aedes transform
service; \code{"cloudvolume"} delegates to \code{\link[fafbseg:flywire_xyz2id]{fafbseg::flywire_xyz2id()}}.}

\item{...}{Additional arguments passed to backend helpers.}
}
\value{
A vector of segment IDs (\code{character} or \code{integer64}).
}
\description{
Find Aedes root or supervoxel (leaf) IDs for XYZ locations
}
\details{
Method auto (which maps to spine) should be much faster for look ups
with many points, especially points in the same region of space.
}
\examples{
\dontrun{
aedes_xyz2id(c(24606, 12450, 5798), rawcoords = TRUE, root = FALSE)
}
}
