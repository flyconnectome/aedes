% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aedes_soma_l2_stats.R
\docType{data}
\name{aedes_soma_l2_stats}
\alias{aedes_soma_l2_stats}
\title{Population L2-attribute model for soma identification}
\format{
A \code{list} with elements:
\describe{
\item{feature_names}{Character vector of the 5 shape features used in
the Mahalanobis distance.}
\item{feature_transforms}{Named list documenting the raw -> feature
transforms (mostly \code{log1p}).}
\item{positive_mean, positive_cov}{Mean vector and covariance matrix of
the positive (KC) training set in feature space.}
\item{positive_quantiles}{Per-feature P5..P95 quantiles for diagnostics.}
\item{dist_npil_density}{List with \code{x} (um grid), \code{y} (mixed density),
\code{y_max}, \code{support}, \code{uniform_density}, \code{uniform_eps}, \code{n} -- the KDE
prior on signed neuropil distance.}
\item{dist_penalty_weight}{Multiplier on the distance-penalty term.}
\item{n_positive, n_negative}{Training set sizes.}
\item{training_set}{Provenance of positives / negatives / distance prior.}
\item{build_date}{Date the dataset was built.}
}
}
\usage{
aedes_soma_l2_stats
}
\description{
Statistics learned from a KC-based training set, used by
\code{\link[=aedes_soma_position]{aedes_soma_position()}} (when the cascade falls through to \code{"l2+mesh"} /
\code{"l2"} / \code{"mesh"}) to score how soma-like each L2 chunk of a neuron is.
}
\details{
The score combines two terms:
\describe{
\item{shape}{Squared Mahalanobis distance of the L2 chunk's five shape
features (\code{log_area_nm2}, \code{log_size_nm3}, \code{log_max_dt_nm},
\code{log_mean_dt_nm}, \code{roundness = pca_val_2 / pca_val_0}) versus the KC
positive population (\code{positive_mean}, \code{positive_cov}). Smaller =
closer to a typical KC soma.}
\item{dist_npil penalty}{\code{-2 * log(f_hat(d_um) / max(f_hat))} where
\code{f_hat} is the empirical KDE of signed neuropil distance over the full
\code{flywire_nuclei()} population, mixed with a small uniform background so
no value is hard-rejected (real central-brain neurons sit 0-3 um inside
the neuropil). Weight controlled by \code{dist_penalty_weight}.}
}
Combined as \code{soma_score = mahal_shape + dist_penalty_weight * dist_penalty}
(lower = more soma-like).
}
\seealso{
\code{\link[=aedes_soma_position]{aedes_soma_position()}}; \code{data-raw/aedes_soma_l2_stats.R} for the
build script.
}
\keyword{datasets}
