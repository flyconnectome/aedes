% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edits-movie.R
\name{aedes_edit_movie}
\alias{aedes_edit_movie}
\title{Animate Aedes proofreading edits as a gif}
\usage{
aedes_edit_movie(
  dat,
  name = "aedes_frames",
  fps = 10,
  duration = 30,
  size = 1.5,
  width = 800,
  height = 600,
  rotate = FALSE,
  bg = "white",
  gif_file = paste0(name, ".gif")
)
}
\arguments{
\item{dat}{A data frame of edit operations with columns \code{timestamp},
\code{user_affiliation}, \code{source_x}, \code{source_y}, \code{source_z}.}

\item{name}{Stem used for the temporary frame directory and default gif
filename.}

\item{fps}{Frames per second (default 10).}

\item{duration}{Duration in seconds (default 30).}

\item{size}{Point size for \code{\link[rgl:primitives]{rgl::points3d()}}.}

\item{width, height}{Output gif dimensions in pixels.}

\item{rotate}{If \code{TRUE}, slowly rotate the camera (half turn) over the
duration. If \code{FALSE} (default), use a fixed frontal view.}

\item{bg}{Background colour (default \code{"white"}).}

\item{gif_file}{Output gif path. Defaults to \code{paste0(name, ".gif")}.}
}
\value{
The output gif path, invisibly.
}
\description{
Animate Aedes proofreading edits as a gif
}
\examples{
\dontrun{
# Fetch edit operations and add user metadata
ops <- aedes:::aedes_all_operations()
ops <- aedes:::add_operation_centroids(ops)
user_info <- googlesheets4::read_sheet(
  "https://docs.google.com/spreadsheets/d/1W0CLjIvgX9rl4ttqgjI3xr3zlL_hA86ZRW0-3-CGQTU/"
)
edit_dat <- ops \%>\% dplyr::left_join(user_info, by = c("user" = "user_id"))

# All edits with rotation
aedes_edit_movie(edit_dat, name = "all_edits", rotate = TRUE)

# Human edits only
edit_dat \%>\%
  dplyr::filter(user_affiliation != "auto") \%>\%
  aedes_edit_movie(name = "human_edits")
}
}
\keyword{internal}
